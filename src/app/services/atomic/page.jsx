import AtomicClient from '../../../components/services/AtomicClient';
import { useT } from '../../../lib/ruMessages';
import './page.css';

export const metadata = { title: 'IT | CIS ENERGY' };

export default function ITPage() {
  return <AtomicClient />;
}
