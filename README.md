# CIS ENERGY — лендинг компании инженерных и IT‑решений для энергетики

Современный одностраничный сайт (Next.js 15 + React 19) для консалтинговой компании в энергетическом секторе. Сайт презентует услуги для **нефтегазовой**, **горнодобывающей**, **атомной отраслей** и **IT‑направления**, демонстрирует доверие через логотипы клиентов и ведёт к конверсии через контактный блок.

---

## ✨ Ключевые особенности

- **Next.js 15 (App Router) + React 19** — современный стек, SSR/CSR‑компоненты.
- **Готовые секции**: Hero, О компании, Услуги (4 направления), Почему мы, Клиенты, Контакты, футер.
- **Отдельные страницы услуг**: `/services/oil-gas`, `/services/mining`, `/services/atomic`, `/services/it` (+ страница `pso/`).
- **Локализация RU (файлы `src/messages/ru.json`)** с лёгким расширением под KK/EN.
- **Адаптивная верстка** и сохранённый исходный дизайн/классы.
- **Статические ассеты** в `public/` и небольшие UI‑скрипты в `public/scripts`.

---

## 🧭 Структура

```
src/
  app/
    layout.js           # метаданные, шапка/подвал, глобальные шрифты
    page.js             # главная страница
    styles/             # стили секций (hero, header, services, why, footer, contact и др.)
    services/           # страницы направлений (oil-gas, mining, atomic, it)
    pso/                # дополнительная лендинговая страница
  components/           # разметка секций (Hero, About, Services, Why*, Logos*, Contact, Footer, Header)
  lib/                  # вспомогательные утилиты (примитивная i18n обёртка)
  messages/             # тексты (ru.json + заготовки)
public/
  images/               # графика (герой, блоки, лого клиентов, иконки)
  scripts/              # мини‑скрипты для UI: логотипы‑карусель, загрузка файла в форме и т.п.
```

---

## 🚀 Быстрый старт

Требования: **Node.js ≥ 18.18 (рекомендуется 20+)**, **npm ≥ 10**.

```bash
npm i
npm run dev
# открыть http://localhost:3000
```

Скрипты:

- `npm run dev` — режим разработки
- `npm run build` — сборка
- `npm run start` — запуск продакшен‑сборки

> В проекте нет серверной обработки формы (только UI). Для полноценной отправки добавьте API‑роут (Next.js Route Handler) или подключите сторонний сервис форм/почты.

---

## ⚙️ Конфигурация и технологии

- **next.config.mjs** — базовая конфигурация Next.js.
- **Глобальные шрифты** подключены в `layout.js` (Google Fonts).
- **Стили** — обычные CSS‑файлы по секциям.
- **Локализация** — в текущей версии подключён RU через `src/lib/ruMessages.js` и `src/messages/ru.json`.
  - Для полноценной мультиязычности рекомендуется `next-intl` + сегмент `[locale]`.

---

## 🧩 Страницы и якоря

- Главная: `/` — секции `#about`, `#services`, `#contact` для навигации из хедера/футера.
- Услуги:
  - `/services/oil-gas` — Нефтегазовая отрасль
  - `/services/mining` — Горнодобывающая отрасль
  - `/services/atomic` — Атомная энергетика
  - `/services/it` — IT‑решения
- Доп. страница: `/pso`

## 📝 Описание проекта (для GitHub)

> **CIS ENERGY** — лендинг консалтинговой компании, предлагающей научные, инженерные и IT‑решения для энергетического комплекса. На сайте представлены направления: нефтегаз, горнодобыча, атомная энергетика и IT‑сервисы. Страница демонстрирует преимущества компании, логотипы клиентов и содержит контактную форму для быстрой связи.

Ключевые секции: **Hero**, **О компании**, **Услуги**, **Почему мы**, **Клиенты**, **Контакты**.

---

## 🧪 Проверка перед деплоем

- `npm run build` должен завершаться без ошибок/варнингов.
- Проверить адаптив: 390 / 768 / 1024 / 1440 / 1920 px.
- Прогнать Lighthouse (Performance/SEO/Best Practices/Accessibility).

---

## 📦 Лицензия

Проект предоставлен для портфолио. Сайт принадлежит компании "MANGO DEVELOPMENT"
